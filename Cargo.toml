[package]
name = "hm_cx"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.0"  # or "rocket" if you prefer that framework
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"